import React from "react";
import { Text, View, Image} from "react-native";
import { format } from "date-fns";
import { IList } from "../../common/validation/list";
function ListCard({
  list
   
}: {
    list : IList
  }) {
  return (
    <View className="flex  flex-row mb-4 items-start">
    
    </View>
  );
}
export default ListCard;
  