test("Render Calendar", () => {
    expect(1 + 1).toBe(2);
  });

test("function Create Event ", () => {
    expect(1 + 1).toBe(2);
  });

test("delete event", () => {
    expect(1 + 1).toBe(2);
  });
  
  
  